apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ingress.yaml
  - middleware-forward-auth.yaml
  - middleware-forward-authz.yaml
  - middleware-errors-auth.yaml
  - middleware-errors-authz.yaml
  - middleware-chain.yaml
