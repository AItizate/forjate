apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: security-cert
spec:
  secretName: tls-secret
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
  - auth.localhost
