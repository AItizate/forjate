apiVersion: v1
kind: Service
metadata:
  name: cluster-introspector
  labels:
    app: cluster-introspector
spec:
  selector:
    app: cluster-introspector
  ports:
    - protocol: TCP
      port: 18789
      targetPort: 18789
      name: http
