apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - deployment.yaml
  - pvc.yaml
  - service.yaml
  # Postgres is opt-in — overlays add ./postgres explicitly if they need it.
