apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ai-tools-cert
spec:
  secretName: tls-secret
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
    - open-webui.localhost
