apiVersion: v1
kind: Service
metadata:
  name: otel-collector-cluster
  labels:
    app.kubernetes.io/name: otel-collector
    app.kubernetes.io/component: cluster
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: otel-collector
    app.kubernetes.io/component: cluster
  ports:
    - name: metrics
      port: 8888
      targetPort: 8888
      protocol: TCP
