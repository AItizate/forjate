apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-local-path-template
  labels:
    disk: template
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  local:
    path: /mnt/ait/local-path
