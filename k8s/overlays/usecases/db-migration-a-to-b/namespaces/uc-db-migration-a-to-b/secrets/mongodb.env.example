# Target database credentials. Dev-only values for a throwaway environment —
# copied to mongodb.env by the runner (and by CI) when that file is missing.
MONGO_INITDB_ROOT_USERNAME=app
MONGO_INITDB_ROOT_PASSWORD=ephemeral-local-only
