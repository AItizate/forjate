# Source database credentials. Dev-only values for a throwaway environment —
# copied to postgres.env by the runner (and by CI) when that file is missing.
POSTGRES_DB=appdb
POSTGRES_USER=app
POSTGRES_PASSWORD=ephemeral-local-only
